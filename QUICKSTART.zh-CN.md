# DrugReflector 快速使用

## 这个工具是干嘛的

DrugReflector 用来做科研里的候选药物优先级筛选。

你给它一个转录组 signature：

- 可以是已经整理好的 `v-score`
- 也可以是一个预处理后的 `.h5ad` 文件

它会输出：

- 候选化合物排名 `rank`
- 原始模型分数 `logit`
- 归一化后的相对概率 `prob`

最适合的场景是：

- 已经有疾病 vs 对照、状态 A vs 状态 B 的表达差异特征
- 想快速得到一批优先验证的候选药物

不适合直接替代：

- 单细胞原始数据质控
- 完整差异分析流程
- 生物学验证

## 最简单的启动方式

在项目根目录直接双击：

- `start_ui.bat`

或者在 PowerShell 里运行：

```powershell
.\start_ui.ps1
```

启动后浏览器会自动打开：

- `http://127.0.0.1:8000/`

如果 `8000` 端口已被占用，脚本会自动换一个可用端口，例如 `8010`，并在终端里打印最终地址。

## 页面里怎么用

### 方法 1：Paste V-scores

把基因和分数按下面格式粘进去：

```text
TP53,1.2
EGFR,-0.4
CDKN1A,0.8
```

然后点击 `Run V-score Prediction`。

### 方法 2：Upload H5AD

上传一个 `.h5ad` 文件，然后点击 `Run H5AD Prediction`。

## 结果怎么看

- `rank` 越小越靠前，`0` 表示当前结果里排第一
- `logit` 越大通常表示模型越偏向该化合物
- `prob` 是模型输出归一化后的相对值

建议把结果当作候选清单，而不是最终结论。

## 导出

页面结果区支持直接下载：

- `CSV`
- `JSON`

## 停止服务

在项目根目录运行：

```powershell
.\stop_ui.ps1
```
